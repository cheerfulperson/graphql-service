input GenreInput {
    name: String
    description: String
    country: String
    year: Int
}

type Genre {
    id: ID!
    name: String
    description: String
    country: String
    year: Int
}

type Query {
    genres(limit: Int): [Genre]
    genre(id: ID!): Genre
}